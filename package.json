{
  "name": "pi-bg",
  "version": "0.1.0",
  "description": "Background processes and external-condition waits for pi: run a server, wait for CI, without blocking a turn.",
  "private": true,
  "keywords": ["pi-package"],
  "license": "MIT",
  "pi": { "extensions": ["./extensions"] },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  }
}
